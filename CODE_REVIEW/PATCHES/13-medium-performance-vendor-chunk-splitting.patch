--- a/vite.config.ts
+++ b/vite.config.ts
@@ -47,6 +47,30 @@
   build: {
     rollupOptions: {
       output: {
+        manualChunks: {
+          // Core React libraries
+          'react-vendor': ['react', 'react-dom', 'react-router-dom'],
+
+          // UI library (Radix UI is heavy)
+          'radix-ui': [
+            '@radix-ui/react-accordion',
+            '@radix-ui/react-alert-dialog',
+            '@radix-ui/react-avatar',
+            '@radix-ui/react-checkbox',
+            '@radix-ui/react-dialog',
+            '@radix-ui/react-dropdown-menu',
+            '@radix-ui/react-label',
+            '@radix-ui/react-popover',
+            '@radix-ui/react-select',
+            '@radix-ui/react-tabs',
+            '@radix-ui/react-toast'
+          ],
+
+          // Supabase and React Query
+          'supabase-vendor': ['@supabase/supabase-js', '@tanstack/react-query'],
+
+          // Heavy dependencies (3D, animations)
+          'three-vendor': ['three', '@react-three/fiber', '@react-three/drei', 'vanta']
+        },
         // Add version to chunk names for better cache busting
         chunkFileNames: (chunkInfo) => {
           const facadeModuleId = chunkInfo.facadeModuleId ? chunkInfo.facadeModuleId.split('/').pop() : 'chunk';
@@ -56,6 +80,5 @@
         assetFileNames: 'assets/[name]-[hash].[ext]'
       },
     },
-    chunkSizeWarningLimit: 1000, // Increase limit to 1MB for now
   },
 }));
